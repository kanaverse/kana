const Stats = (props) => {

    return (
        <>
            {
                <span>
                    {props?.headerInfo}
                </span>
            }
        </>
    );
};

export default Stats;
